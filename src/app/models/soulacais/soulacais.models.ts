import { User } from '../user/user.model';

export class Soulacais {
  id: number;
  user: User;
  img: string;
  sex: boolean;
  weight: number;
  resistance: number;
  alcohol: number;
  trend: string;
}
